spring:
   datasource:
      driver-class-name: org.h2.Driver
      jdbcUrl: jdbc:h2:mem:testdb;
      username: sa
      password:
      hikari:
         poolName: pool
#   mvc:
#      pathmatch:
#         matching-strategy: ant_path_matcher